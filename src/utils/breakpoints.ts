/**
 * Where the header nav stops fitting on one line. Measured, not rounded: below
 * this the five numbered links plus the contact pill wrap, and the sticky
 * header grows to several rows.
 */
export const COMPACT_NAV_QUERY = '(max-width: 860px)';
